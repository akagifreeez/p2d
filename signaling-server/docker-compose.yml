version: '3.8'

services:
  signaling-server:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: p2d-signaling
    ports:
      - "8080:8080"
    restart: unless-stopped
    environment:
      - NODE_ENV=production
