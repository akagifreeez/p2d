name = "p2d-signaling"
main = "src/index.ts"
compatibility_date = "2024-09-23"

# ルームごとの参加者上限 (既定8)。varsで上書き
[vars]
MAX_PARTICIPANTS = "8"

[[durable_objects.bindings]]
name = "ROOM"
class_name = "RoomDurableObject"

[[migrations]]
tag = "v1"
new_sqlite_classes = ["RoomDurableObject"]

# 独自ドメイン / アカウントIDは deploy 時に wrangler が案内
# (詳細は README.md)
